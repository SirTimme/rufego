package main;

type IMessage interface {
    send() IMessage
    receive() message
}

type client struct {
    msg message
}

type message struct {}

func (client client) send() IMessage {
    return client{ message{} }
}

func (client client) receive() message {
    return message{}
}

func main() {
    _ = message{}
}